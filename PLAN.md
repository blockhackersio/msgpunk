- In the form listing in the client app we need a bottom tab menu to show the following:
    - the form url with a copy button
    - embed button code with a copy button
        - This should be a button that other people can host on their sites that open the form in an iframe in a drawer the same as on the home page of the server. 
        - The code should be minimal and defer to a preexisting script that can simply be included in the page or follow the best practice for this sort of thing. 
        - if it is really complex then instead of doing something complex simply link out to the form in a new window
    - This should probably be a menu accessible from the bottom bar of the application when on the form results 
page
