{
  "name": "msgpunk-server",
  "private": true,
  "scripts": {
    "build": "pnpm --filter @msgpunk/toolkit run build && pnpm --filter form-page run build",
    "test:flow": "bash scripts/test-flow.sh"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2"
  },
  "pnpm": {
    "overrides": {
      "baseline-browser-mapping": "2.11.20"
    }
  }
}
